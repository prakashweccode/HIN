import { Opportunityacquisitioncost } from './opportunityacquisitioncost';

describe('Opportunityacquisitioncost', () => {
  it('should create an instance', () => {
    expect(new Opportunityacquisitioncost()).toBeTruthy();
  });
});

import { Proposalclosedbyrep } from './proposalclosedbyrep';

describe('Proposalclosedbyrep', () => {
  it('should create an instance', () => {
    expect(new Proposalclosedbyrep()).toBeTruthy();
  });
});
